periodics:

- name: periodic-testing-close
  interval: 1h
  cluster: trusted
  decorate: true
  annotations:
    testgrid-dashboards: jetstack-testing-janitors
    testgrid-alert-email: cert-manager-dev-alerts@googlegroups.com
    description: Closes PRs and issues that are marked 'rotten' and have been inactive for 30d
  labels:
    preset-deployer-github-token: "true"
  spec:
    containers:
    - image: gcr.io/k8s-prow/commenter:v20230411-7b4f9b177a
      command:
      - commenter
      args:
      - |-
        --query=repo:cert-manager/cert-manager
        -label:lifecycle/frozen
        label:lifecycle/rotten
      - --updated=720h
      - --token=/etc/github/token
      - |-
        --comment=Rotten issues close after 30d of inactivity.
        Reopen the issue with `/reopen`.
        Mark the issue as fresh with `/remove-lifecycle rotten`.
        Send feedback to [jetstack](https://github.com/jetstack).
        /close
      - --ceiling=10
      - --confirm

- name: periodic-testing-rotten
  interval: 1h
  cluster: trusted
  decorate: true
  annotations:
    testgrid-dashboards: jetstack-testing-janitors
    testgrid-alert-email: cert-manager-dev-alerts@googlegroups.com
    description: Marks PRs and issues that are marked 'stale' and have been inactive for 30d as 'rotten'
  labels:
    preset-deployer-github-token: "true"
  spec:
    containers:
    - image: gcr.io/k8s-prow/commenter:v20230411-7b4f9b177a
      command:
      - commenter
      args:
      - |-
        --query=repo:cert-manager/cert-manager
        -label:lifecycle/frozen
        label:lifecycle/stale
        -label:lifecycle/rotten
      - --updated=720h
      - --token=/etc/github/token
      - |-
        --comment=Stale issues rot after 30d of inactivity.
        Mark the issue as fresh with `/remove-lifecycle rotten`.
        Rotten issues close after an additional 30d of inactivity.
        If this issue is safe to close now please do so with `/close`.
        Send feedback to [jetstack](https://github.com/jetstack).
        /lifecycle rotten
        /remove-lifecycle stale
      - --ceiling=10
      - --confirm

- name: periodic-testing-stale
  interval: 1h
  cluster: trusted
  decorate: true
  annotations:
    testgrid-dashboards: jetstack-testing-janitors
    testgrid-alert-email: cert-manager-dev-alerts@googlegroups.com
    description: Marks PRs and issues that have been inactive for 30d as 'stale'
  labels:
    preset-deployer-github-token: "true"
  spec:
    containers:
    - image: gcr.io/k8s-prow/commenter:v20230411-7b4f9b177a
      command:
      - commenter
      args:
      - |-
        --query=repo:cert-manager/cert-manager
        -label:lifecycle/frozen
        -label:lifecycle/stale
        -label:lifecycle/rotten
      - --updated=2160h
      - --token=/etc/github/token
      - |-
        --comment=Issues go stale after 90d of inactivity.
        Mark the issue as fresh with `/remove-lifecycle stale`.
        Stale issues rot after an additional 30d of inactivity and eventually close.
        If this issue is safe to close now please do so with `/close`.
        Send feedback to [jetstack](https://github.com/jetstack).
        /lifecycle stale
      - --ceiling=10
      - --confirm

- name: periodic-testing-autobump
  cron: "30 18-23/5 * * 1-5"  # Run at 10:30 and 15:30 PST (18:05 UTC, fall) Mon-Fri
  cluster: trusted
  decorate: true
  annotations:
    testgrid-dashboards: jetstack-testing-janitors
    testgrid-alert-email: cert-manager-dev-alerts@googlegroups.com
    description: Creates autobump PRs for the jetstack/testing repo.
  extra_refs:
  - org: jetstack
    repo: testing
    base_ref: master
  labels:
    preset-deployer-github-token: "true"
    preset-deployer-ssh-key: "true"
  spec:
    containers:
    - image: gcr.io/k8s-prow/generic-autobumper:v20230411-7b4f9b177a
      command:
      - generic-autobumper
      args:
      - --config=config/autobump-config/testing-autobump-config.yaml

- name: periodic-testing-label-sync
  cron: "17 * * * *"  # Every hour at 17 minutes past the hour
  cluster: trusted
  decorate: true
  annotations:
    testgrid-dashboards: jetstack-testing-janitors
    testgrid-alert-email: cert-manager-dev-alerts@googlegroups.com
    description: Runs label_sync to synchronize GitHub repo labels with the label config defined in label_sync/labels.yaml.
  extra_refs:
  - org: jetstack
    repo: testing
    base_ref: master
  labels:
    preset-deployer-github-token: "true"
  spec:
    containers:
    - name: label-sync
      image: gcr.io/k8s-prow/label_sync:v20230412-a86d65c3c2
      command:
      - label_sync
      args:
      - --config=config/labels.yaml
      - --confirm=true
      # TODO: enable label_sync across the whole org
      - --only=cert-manager/cert-manager,jetstack/testing,cert-manager/release,cert-manager/webhook-example,cert-manager/website,cert-manager/csi-lib,cert-manager/approver-policy,cert-manager/csi-driver,cert-manager/istio-csr,cert-manager/csi-driver-spiffe,cert-manager/infrastructure
      - --token=/etc/github/token
      - --endpoint=https://api.github.com
      - --debug

- name: periodic-testing-branchprotector
  cron: "54 * * * *"  # Every hour at 54 minutes past the hour
  cluster: trusted
  decorate: true
  decoration_config:
    timeout: 5h
  annotations:
    testgrid-dashboards: jetstack-testing-janitors
    testgrid-alert-email: cert-manager-dev-alerts@googlegroups.com
    description: Runs Prow's branchprotector to apply configured GitHub status context requirements and merge policies.
  extra_refs:
  - org: jetstack
    repo: testing
    base_ref: master
  labels:
    preset-deployer-github-token: "true"
  spec:
    containers:
    - name: branchprotector
      image: gcr.io/k8s-prow/branchprotector:v20230412-a86d65c3c2
      command:
      - branchprotector
      args:
      - --config-path=config/config.yaml
      - --job-config-path=config/jobs
      - --github-token-path=/etc/github/token
      - --confirm
      - --github-endpoint=https://api.github.com
